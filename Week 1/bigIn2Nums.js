//  Find the big number in given two numbers

//Declaring and initializing numbers to the variables
let firstNum=10,secondNum=20;

// Check whether firstNum is greater than secondNum
if(firstNum>secondNum){
    console.log(`${firstNum} is greater than ${secondNum}`) // Executes if firstNum is greater
}
else{
    console.log(`${secondNum} is greater than ${firstNum}`) // Executes if the above condition is false
}
